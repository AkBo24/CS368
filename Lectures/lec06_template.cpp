#include <iostream>
#include <string>
#include <iomanip>

using namespace std;


int main() {

    // declare primitives
    int x  = 3, y = 5;
    int *p = &x;

    // declare p as a pointer to x
    cout << "x" << " address of x" << &x << endl
         << "y" << " address of y" << &y << endl
         << "p" << " *p = " << *p << " address of p = " << &p << endl;

    // modify x

    // modify x through *p

    // reassign p to point to y
    
    // modify *p again

    // references
    
    // primitive arrays

    // dynamically allocated arrays

    // Character Arrays are Special
         
    // Understanding Complicated Array Declarations
            
    // Multidimensional arrays
     
    // Indexing into multidimensional arrays
  
    // Bind a row of a multidimensional array to a variable
   
    // Range for loop with multidimensional arrays
  
    // Parameter Passing
    
    // Passing Primitive Arrays to Functions
}
